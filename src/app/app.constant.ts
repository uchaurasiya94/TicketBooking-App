export enum CommonConstants {
    ONE_DAY = 1,
    THIRTY_DAYS = 30
}

export enum StatusCodes {
    INTERNAL_ERROR = 500,
    RESULTS_FOUND = 200,
    RECORD_MODIFIED = 201,
    RECORD_DELETED = 204,
    NO_RESULT_FOUND = 404
}